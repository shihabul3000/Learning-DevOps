এই কোডটি basically একটি Node.js server বানাচ্ছে, কিন্তু TypeScript ব্যবহার করে।
এবং .env ফাইল থেকে environment variable লোড করছে।

✅ লাইন বাই লাইন সহজ ব্যাখ্যা (Bangla)
1️⃣
import { config } from "dotenv";


এটি .env ফাইলের ভেতরের variables (যেমন PORT, DATABASE_URL) লোড করার জন্য ব্যবহৃত হয়।

2️⃣
import http, { IncomingMessage, Server, ServerResponse } from "http";


http → Node.js এর built-in module, যেটা server বানাতে লাগে।

IncomingMessage → req এর টাইপ (request)

ServerResponse → res এর টাইপ (response)

Server → সার্ভারের টাইপ

3️⃣ সার্ভার তৈরি করা
const server : Server = http.createServer((req : IncomingMessage , res : ServerResponse) => {


এখানে server তৈরি করা হচ্ছে।

req = client কী চেয়েছে
res = server কী রেসপন্স দেবে

4️⃣ রাউট চেক করা
if(req.url == "/" && req.method == "GET"){


মানে:

ইউজার যদি ব্রাউজারে শুধু / (home page) লেখে

এবং GET method ব্যবহার করে

তাহলে নিচের response পাঠাবে।

5️⃣ রেসপন্স পাঠানো
res.writeHead(200, {"content-type" : "application/json"} );


200 → সফল response

application/json → রেসপন্স JSON ফরম্যাটে হবে

তারপর:

res.end(
    JSON.stringify({
        message: "Hellow From Node js with typesript.....",
        path: req.url,
    })
);


এটি JSON আকারে response পাঠায়।

6️⃣ Server listen
server.listen(5000,() => {
    console.log(`Server is Running on Port${5000}`)
});


এখানে server 5000 port এ চালু হয়।

🔥 এখন dotenv সঠিকভাবে ব্যবহার করা অংশ
import dotenv from "dotenv";
import path from "path";

dotenv.config({path : path.join(process.cwd(),".env")});


এখানে .env ফাইল লোড করা হচ্ছে।

path.join(process.cwd(), ".env")
→ আপনার project-এর root folder থেকে .env ফাইলটা খুঁজে লোড করবে।

এভাবে .env এর ভেতরের variable গুলো ব্যবহার করতে পারবেন:

PORT=5000


এবং কোডে:

process.env.PORT

🎯 সারাংশ (Super Simple)
অংশ	কাজ
http.createServer	Node.js সার্ভার তৈরি করা
req	রিকোয়েস্ট ডেটা
res	রেসপন্স পাঠানো
/ এবং GET চেক	হোমপেজ রিকোয়েস্ট কিনা চেক
res.writeHead	রেসপন্স হেডার সেট করা
res.end	রেসপন্স পাঠানো
dotenv	.env ফাইল লোড করা